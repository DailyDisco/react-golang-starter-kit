name: Complete CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

# Cancel in-progress runs for the same workflow and branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "20"
  GO_VERSION: "1.25"
  FRONTEND_DIR: ./frontend
  BACKEND_DIR: ./backend

jobs:
  # Frontend Security Check
  frontend-security:
    name: Frontend Security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Run security audit
        run: npm audit --audit-level=high
        working-directory: ${{ env.FRONTEND_DIR }}

  # Frontend Quality Checks
  frontend-quality:
    name: Frontend Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Check formatting
        run: npm run prettier:check
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Run linter
        run: npm run lint
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Run type checking
        run: npm run typecheck
        working-directory: ${{ env.FRONTEND_DIR }}

  # Frontend Testing
  frontend-test:
    name: Frontend Test (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['20', '22']
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
          cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Run tests with coverage
        run: npm run test:coverage
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Upload coverage report
        uses: actions/upload-artifact@v6
        with:
          name: frontend-coverage-node-${{ matrix.node-version }}
          path: ${{ env.FRONTEND_DIR }}/coverage/
          retention-days: 7

  # Frontend E2E Testing (Playwright)
  frontend-e2e:
    name: Frontend E2E
    runs-on: ubuntu-latest
    needs: [frontend-build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Download frontend build
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: ${{ env.FRONTEND_DIR }}/dist/

      - name: Run Playwright tests
        run: npm run test:e2e
        working-directory: ${{ env.FRONTEND_DIR }}
        env:
          CI: true
          PLAYWRIGHT_BASE_URL: http://localhost:3000

      - name: Upload Playwright report
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: playwright-report
          path: ${{ env.FRONTEND_DIR }}/playwright-report/
          retention-days: 30

      - name: Upload Playwright results
        uses: actions/upload-artifact@v6
        if: failure()
        with:
          name: playwright-results
          path: ${{ env.FRONTEND_DIR }}/playwright-results/
          retention-days: 7

  # Backend Linting and Formatting
  backend-lint:
    name: Backend Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6 # Upgrade to v6
        with:
          version: latest
          working-directory: ${{ env.BACKEND_DIR }}
          args: --timeout=5m
          install-mode: go-install # Build from source with your Go version

  # Backend Swagger Validation
  backend-swagger:
    name: Backend Swagger
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install swag
        run: go install github.com/swaggo/swag/cmd/swag@v1.16.6

      - name: Validate Swagger docs
        run: |
          mkdir -p /tmp/swagger-validate
          swag init -g cmd/main.go -o /tmp/swagger-validate
          sed -i '/LeftDelim:/d; /RightDelim:/d' /tmp/swagger-validate/docs.go

          if ! diff -q docs/swagger.json /tmp/swagger-validate/swagger.json; then
            echo "ERROR: Swagger docs outdated!"
            echo "Run: cd backend && make swagger"
            diff docs/swagger.json /tmp/swagger-validate/swagger.json || true
            exit 1
          fi
          echo "Swagger docs up-to-date."
        working-directory: ${{ env.BACKEND_DIR }}

  # Backend Security
  backend-security:
    name: Backend Security
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Run govulncheck
        run: |
          if command -v govulncheck >/dev/null 2>&1; then
            govulncheck ./...
          else
            go install golang.org/x/vuln/cmd/govulncheck@latest
            govulncheck ./...
          fi
        shell: bash
        working-directory: ${{ env.BACKEND_DIR }}

  # Backend Testing
  backend-test:
    name: Backend Test (Go ${{ matrix.go-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.24', '1.25']
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          cache: true

      - name: Run tests with coverage
        run: |
          echo "🧪 Running Go tests with race detector..."
          if find . -name "*_test.go" -type f | grep -q .; then
            go test -v -race -timeout 5m -coverprofile=coverage.out -covermode=atomic ./...
            go tool cover -func=coverage.out
          else
            echo "✅ No Go test files found - skipping tests"
          fi
        shell: bash
        working-directory: ${{ env.BACKEND_DIR }}

      - name: Upload coverage report
        uses: actions/upload-artifact@v6
        if: success() && hashFiles('backend/coverage.out') != ''
        with:
          name: backend-coverage-go-${{ matrix.go-version }}
          path: ${{ env.BACKEND_DIR }}/coverage.out
          retention-days: 7

  # Backend Migrations Test
  backend-migrations:
    name: Backend Migrations
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: testdb
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install golang-migrate
        run: go install -tags 'postgres' github.com/golang-migrate/migrate/v4/cmd/migrate@latest

      - name: Run migrations up
        run: migrate -path ./migrations -database "postgres://testuser:testpass@localhost:5432/testdb?sslmode=disable" up
        working-directory: ${{ env.BACKEND_DIR }}

      - name: Run migrations down
        run: migrate -path ./migrations -database "postgres://testuser:testpass@localhost:5432/testdb?sslmode=disable" down -all
        working-directory: ${{ env.BACKEND_DIR }}

      - name: Re-run migrations (idempotency check)
        run: migrate -path ./migrations -database "postgres://testuser:testpass@localhost:5432/testdb?sslmode=disable" up
        working-directory: ${{ env.BACKEND_DIR }}

      - name: Verify final state
        run: migrate -path ./migrations -database "postgres://testuser:testpass@localhost:5432/testdb?sslmode=disable" version
        working-directory: ${{ env.BACKEND_DIR }}

  # Frontend Build
  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest
    needs: [frontend-security, frontend-quality, frontend-test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: ${{ env.FRONTEND_DIR }}/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Build frontend
        run: npm run build
        working-directory: ${{ env.FRONTEND_DIR }}

      - name: Upload frontend build
        uses: actions/upload-artifact@v6
        with:
          name: frontend-build
          path: ${{ env.FRONTEND_DIR }}/dist/
          retention-days: 30

  # Backend Build
  backend-build:
    name: Backend Build
    runs-on: ubuntu-latest
    needs: [backend-lint, backend-security, backend-test, backend-migrations, backend-swagger]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Build backend
        run: go build -ldflags="-s -w" -o backend-linux-amd64 ./cmd/main.go
        working-directory: ${{ env.BACKEND_DIR }}

      - name: Upload backend build
        uses: actions/upload-artifact@v6
        with:
          name: backend-build
          path: ${{ env.BACKEND_DIR }}/backend-linux-amd64
          retention-days: 30

  # Final summary
  summary:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [frontend-build, backend-build, frontend-e2e]
    if: always()
    steps:
      - name: Generate summary
        run: |
          echo "## 🚀 CI Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend Build | ${{ needs.frontend-build.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Backend Build | ${{ needs.backend-build.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend E2E | ${{ needs.frontend-e2e.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [[ "${{ needs.frontend-build.result }}" == "success" && "${{ needs.backend-build.result }}" == "success" && "${{ needs.frontend-e2e.result }}" == "success" ]]; then
            echo "🎉 **All CI checks passed!**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Some CI checks failed. Please review the failed jobs.**" >> $GITHUB_STEP_SUMMARY
          fi
        shell: bash

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=devuser
DB_PASSWORD=devpass
DB_NAME=devdb
DB_SSLMODE=disable

# Note: When running with Docker Compose, these values will be overridden
# by the docker-compose.yml environment variables
